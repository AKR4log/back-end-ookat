export enum OrderStatus {
	PENDING,
	PAYED,
	SHIPPED,
	DELIVERED
}

export enum Role {
	SUPERADMIN,
	ADMIN,
	CLIENT,
	OPERATOR,
	CONTENTMANAGER,
	ACCOUNTANT,
	SELLER
}

export enum SellerStatus {
	ONCHECK,
	CANCELLED,
	APPROVED,
	DEFICIENT
}

export enum ProductStatus {
	PRIVATE,
	PUBLIC
}
